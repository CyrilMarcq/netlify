name: "ci-cd pipeline"

on: 
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: "Deployed site"
      run: curl -X POST -d {} steady-bienenstitch-95ef47.netlify.app
