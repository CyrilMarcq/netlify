name: "ci-cd pipeline"

on: 
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: "Deployed site"
      run: curl -X POST -d {} db108eed-c887-4e48-a77a-897e884e5dda
